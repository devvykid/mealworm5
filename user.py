import json
import requests

from facebook import MessageElements


class User:
    # 초기화 메서드
    def __init__(self, user_config, config):
        # 디비에서 불러온는 경우는



        # config
        self.config = config

        # 페북 관련
        self.uid = req['sender']['id']
        self.name = None

        return








